#set( $D = '$' )

dependencies {
#foreach ($dependency in $dto.projectDependencies)
   compile project(":$dependency")
#end

   compile "${dto.getFormattedDependency('com.ngc.seaside:service.api')}"
   compile "${dto.getFormattedDependency('com.ngc.seaside:service.transport.api')}" "com.ngc.seaside:service.transport.api:${D}transportServiceVersion"

   compile "${dto.getFormattedDependency('com.ngc.blocs:service.api')}"
   compile "${dto.getFormattedDependency('org.osgi:osgi.core')}"
   compile "${dto.getFormattedDependency('org.osgi:osgi.enterprise')}"

   testCompile "${dto.getFormattedDependency('com.ngc.blocs:test.impl.common.testutilities')}"
   testCompile "${dto.getFormattedDependency('junit:junit')}"
   testCompile "${dto.getFormattedDependency('org.mockito:mockito-core')}"
}
