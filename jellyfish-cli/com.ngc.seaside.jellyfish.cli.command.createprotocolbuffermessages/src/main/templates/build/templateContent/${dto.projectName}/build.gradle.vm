buildscript {
	repositories {
        mavenLocal()

        maven {
            url nexusConsolidated
        }
    }

	dependencies {
		classpath "${build.getFormattedDependency('com.google.protobuf:protobuf-gradle-plugin')}"
	}
}

ext {
    command = 'create-protocolbuffer-messages'
}

apply plugin: 'com.ngc.seaside.jellyfish.system-descriptor-derived'
