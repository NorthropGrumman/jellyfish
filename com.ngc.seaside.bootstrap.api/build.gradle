


dependencies {
    compile project(":command.api")

    testCompile "junit:junit:$junitVersion"
}

jar {
    manifest {
        // Bnd instructions to export the api package.
        attributes('Export-Package': 'com.ngc.seaside.bootstrap.api.**')
    }
}

