// Use Nexus to retrieve additional Gradle plugins used by the build.
buildscript {
   repositories {
      mavenLocal()

      maven {
         url nexusConsolidated
      }
   }

   dependencies {
      classpath 'com.ngc.seaside:gradle.plugins:2.1.0'
      classpath 'org.sonarsource.scanner.gradle:sonarqube-gradle-plugin:2.5'
   }
}

subprojects {
   // Do not make the feature and update site projects Java projects.
   if(name != 'systemdescriptor.ext.feature'
      && name != 'systemdescriptor.ext.updatesite') {
      apply plugin: 'com.ngc.seaside.parent'
   }

   group = 'com.ngc.seaside'
	version = '1.10.1-SNAPSHOT'

   ext {
      jellyfishDslVersion = '0.12.0-SNAPSHOT'
      jellyfishApiVersion = '1.5.1-SNAPSHOT'
      bootstrapApiVersion = '1.7.3-SNAPSHOT'
      guavaVersion = '21.0'
      guiceVersion = '3.0'
      blocsCoreVersion = '2.1'
	  commonsIOVersion = '2.5'
      commonsLangVersion = '2.6'
      glassfishJsonVersion = '1.1'
      emfVersion = '2.12.0'
      xtextVersion = '2.10.0'
      log4jVersion = '1.2.17'
      mavenVersion = '3.5.2'

      junitVersion = '4.12'
      mockitoVersion = '2.7.14'
   }
}
