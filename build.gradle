
apply plugin: "java"

repositories {
    mavenLocal()

	maven {
        url nexusConsolidated
    }
}
    
dependencies {
	//compile project(":jellyfish-cli")
	testCompile group: 'junit', name: 'junit', version: '4.+'
 }

subprojects {
	//apply plugin: 'com.ngc.seaside.parent'
}

