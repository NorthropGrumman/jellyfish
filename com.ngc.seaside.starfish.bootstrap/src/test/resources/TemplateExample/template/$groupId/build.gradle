group '${groupId}'
version '${version}'

apply plugin: 'java'
apply plugin: 'application'

mainClassName = '${groupId}.${artifactId}.Sample'

repositories {
    maven {
		url 'http://10.207.42.43/nexus/repository/maven-central/'
	}
}

dependencies {
    testCompile 'junit:junit:4.12'
}
