
dependencies {
    compile group: 'org.apache.commons', name: 'commons-lang3', version: '3.5'
}

jar {
    manifest {
        // Bnd instructions to export the api package.
        attributes('Export-Package': 'com.ngc.seaside.jellyfish.cli.utilities.*')
    }
}