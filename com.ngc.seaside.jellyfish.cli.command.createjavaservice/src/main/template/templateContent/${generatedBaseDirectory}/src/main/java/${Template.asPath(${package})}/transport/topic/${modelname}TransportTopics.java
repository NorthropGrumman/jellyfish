package ${package}.transport.topic;

import com.ngc.seaside.service.transport.api.ITransportTopic;

/**
 * The transport topics used by the engagement track priority service.
 */
public enum ${modelname}TransportTopics implements ITransportTopic {
   TRACK_ENGAGEMENT_STATUS,
   TRACK_PRIORITY
}
