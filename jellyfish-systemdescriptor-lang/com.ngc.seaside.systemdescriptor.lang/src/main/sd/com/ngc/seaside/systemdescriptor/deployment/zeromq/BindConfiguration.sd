package com.ngc.seaside.systemdescriptor.deployment.zeromq

import com.ngc.seaside.systemdescriptor.deployment.NetworkInterface

data BindConfiguration {
	metadata {
		"description" : "Part of the configuration for ZeroMQ's TCP transport.  Defines how the server like component is configured."
	}

	NetworkInterface interface {
		metadata {
			"description": "The local interface that the server should bind to when listening for connections."
		}
	}
}
