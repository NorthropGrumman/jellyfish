package com.ngc.seaside.systemdescriptor.telemetry

data Telemetry {
	metadata {
		"description" : "Defines the telemetry configuration for a system or service.  Data types can extend this type to enable alternative ways to configure telemetry.  In most cases, one of these specific types of properties are applied to a model instead of applying this type as a property directly.",
		"target": "model"
	}
}
