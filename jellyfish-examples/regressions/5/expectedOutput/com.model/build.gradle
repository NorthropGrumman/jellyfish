buildscript {
    repositories {
        mavenLocal()

        maven {
            credentials {
                username nexusUsername
                password nexusPassword
            }
            url nexusConsolidated
        }
    }

   dependencies {
      classpath "com.google.protobuf:protobuf-gradle-plugin:0.8.3"
      classpath "com.ngc.blocs:gradle.plugin:0.5"
      classpath "com.ngc.seaside:gradle.plugins:2.3.0"
      classpath "com.ngc.seaside:jellyfish.cli.gradle.plugins:2.2.0-SNAPSHOT"
      classpath "org.sonarsource.scanner.gradle:sonarqube-gradle-plugin:2.5"
   }
}

apply plugin: 'com.ngc.seaside.jellyfish.system-descriptor-derived-root'

systemDescriptor {
   project = 'com.ngc.seaside.jellyfish.examples:pubsub.systemdescriptor:2.0.0-SNAPSHOT'
   model = 'com.Model'
}

subprojects {

   // Do not apply the parent plugin to the distribution project.
   if(!name.endsWith('.distribution')
       && !name.endsWith('.docker')) {
      apply plugin: 'com.ngc.seaside.parent'
    }

   group = 'com'
   version = '1.0-SNAPSHOT'

   ext {
      blocsCoreVersion = '2.2.0'
      cucumberVersion = '1.2.5'
      guavaVersion = '22.0'
      guiceVersion = '4.1.0'
      jellyfishVersion = '2.2.0-SNAPSHOT'
      junitVersion = '4.12'
      mockitoVersion = '2.7.14'
      osgiVersion = '6.0.0'
      protobufVersion = '3.2.0'
      starfishVersion = '2.4.0-SNAPSHOT'
   }
}
